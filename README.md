# Kagi Focused NTP

This is a simple Chrome New Tab Page extension that makes https://kagi.com your new tab page and sets focus to the input field instead of the URL bar.

To allow the new tab page to have focus instead of the url bar, the url must have a search query so you will see https://kagi.com/?s= in the url bar instead of it being blank.

That is all it does.